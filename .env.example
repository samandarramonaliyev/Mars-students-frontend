# =============================================================================
# MARS DEVS - FRONTEND ENVIRONMENT VARIABLES
# =============================================================================
# Скопируйте этот файл в .env и заполните значения

# API URL - ВАЖНО: должен включать /api на конце!
#
# Для локальной разработки: /api (используется прокси в vite.config.js)
# Для Docker Compose: /api (nginx проксирует на backend)
# Для Vercel: /api (vercel.json rewrites проксирует на backend)
# Для Render Static Site: https://your-backend-name.onrender.com/api
#
# Примеры:
#   VITE_API_URL=/api                                           # локально / Docker / Vercel
#   VITE_API_URL=https://marsdevs-api.onrender.com/api          # Render
#   VITE_API_URL=https://mars-students-backend.onrender.com/api # прямые запросы
#
# ============================================================================
# VERCEL DEPLOYMENT:
# ============================================================================
# Вариант 1 (рекомендуется): Использовать rewrites из vercel.json
#   VITE_API_URL=/api
#
# Вариант 2: Прямые запросы к backend (требует CORS настройки на backend)
#   VITE_API_URL=https://mars-students-backend.onrender.com/api
#
VITE_API_URL=/api
