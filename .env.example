# =============================================================================
# MARS DEVS - FRONTEND ENVIRONMENT VARIABLES
# =============================================================================
# Скопируйте этот файл в .env и заполните значения

# API URL
# Для локальной разработки: /api (используется прокси в vite.config.js)
# Для production: /api (nginx проксирует на backend)
# Для внешнего API: https://your-backend-domain.com/api
VITE_API_URL=/api
